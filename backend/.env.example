# === LOCAL (mặc định dùng SQLite, không cần điền DATABASE_URL) ===
# Điền CLAUDE_API_KEY nếu muốn dùng nút "Phân tích bằng AI"
CLAUDE_API_KEY=
SECRET_KEY=doi-thanh-chuoi-ngau-nhien

# === PRODUCTION (khi dùng PostgreSQL/Docker hoặc Render) ===
# DATABASE_URL=postgresql://bds:matkhau@db:5432/bds
# PUBLIC_URL=https://api.bds-donganh.example.com
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>   # lấy ở Cloudinary Dashboard > "API Environment variable"
